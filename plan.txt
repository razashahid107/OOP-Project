classes ill need

Class Login{
    setEmail();
    setPassword();
    getEmail();
    getPassword();
    verify(); // verifies if the passwrod and login id are oki from database
}

// new class needed which will inherit from the base class Login 
// this new class will inherit attributes like loginID

class Notes : public Login {
private:
    string noteText;
public:
    setNote(); // variables like noteTitle and (noteDate, not important)
    getNote();
    saveNote(); // saves the note to the database/textfile
}

// to upon researching i get to know k i can add things like cut, paste, copy and delete note etc
// the link of the project to refer is below
// https://github.com/yehiahd/Note-App

// Demo Functions:
    New
    Open
    Print (Pdf)
    Copy
    Cut
    Paste
    Undo/Redo
    Exit 